
 (
     function numerospares(num){
     'use strict'
    debugger
    
    var cont=0;
    var arraypar=[cont];
   
                for(cont=0;cont<=num;cont++){
                    if(cont%2==0){
                        console.log(cont)
                    
                    }else{
 
                    } 
                } 
               
var newarraypar=numerospares(100);
arraypar.push(10);
  return arraypar; 
 // el .push parece que lo emplee mal porque no veo ni pio.
})() 
